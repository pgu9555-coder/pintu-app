{
  "rooms": {
    "read": "auth != null",
    "create": "auth != null && request.data.ownerUid == auth.uid",
    "update": "auth != null && (request.data.ownerUid == null || request.data.ownerUid == doc.ownerUid) && (request.data.code == null || request.data.code == doc.code)",
    "delete": "auth != null && doc.ownerUid == auth.uid"
  }
}
